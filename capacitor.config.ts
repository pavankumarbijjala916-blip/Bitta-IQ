import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.greenbatterybuddy.app',
  appName: 'GreenBatteryBuddy',
  webDir: 'dist'
};

export default config;
